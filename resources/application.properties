# Set the server port (default is 8080)
server.port=8080

# Set the application name (optional)
spring.application.name=java-web-app

# Enable logging at a specific level (optional)
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# Customize context path (optional)
server.servlet.context-path=/app
